# 2026-06-05 Daily Log

### 2026-06-05 数据刷新 #3 (16:24)
- **操作**: generate → push (Git Data API)
- **数据量**: 57,936 条记录，44 Lanes，66 POLs，87 DELs，866 CULs
- **ETD 范围**: 2025-06-11 ~ 2026-07-04 | ETB 范围: 2026-01-04 ~ 2026-08-12
- **db_data.json**: 30.8 MB，Blob SHA=47f1d8b0f4
- **version.txt**: 20260605162400
- **Commit**: 36c977585b
- **部署方式变更**: Contents API (PUT) 对大文件返回 403 → 改用 Git Data API（blob → tree → commit → ref），blob 创建成功但 commit push 也成功
- **⚠️ deploy_data.py 需更新**: 改用 Git Data API 路径以避免 Contents API 的大文件限制

### 2026-06-05 回退到 11:43 BJT 版本 (16:19)
- **用户要求**: 回退到 11:43 BJT 版本（撤销今天所有 UI 改动）
- **恢复** (commit b513965728): index.html → commit `39c1d5cdf6` (55,716 bytes, clean fetch 版本)
- **撤销的改动**: Data Labels / 4项修复（表头/ETB/筛选/POL TOTAL）/ POL toggle / Lane+POL 折叠 / 3级层次 / CUL级ETB筛选
- **保留**: db_data.json 仍是最新数据（不影响）

### 2026-06-05 ETB 筛选改为 CUL 级逻辑 (16:03)
- **需求**: 选择 ETB 日期范围后，若一个 CUL 里有任何一条 booking 的 ETB 在范围内，整个 CUL 的所有 booking 都保留（船不劈开）
- **修复** (commit 2bce25ee4a):
  - `getFilteredData()`: 先遍历 ALL_DATA 建立 `etbCulSet`（有 ETB 在范围内的 CUL 白名单），然后筛选时跳过逐行 ETB 比较，改为 `if (etbCulSet && !etbCulSet[r.cul_code]) return false`
  - `cascadeFilters()`: 同样的 CUL 级逻辑，ETB 筛选后 CUL/POL/DEL 下拉选项也基于白名单生成
- **逻辑**: `for each booking: 有 ETB 在范围内 → 标记 CUL 为有效 → 保留该 CUL 的所有 booking`

### 2026-06-05 3 级折叠层次：Lane → CUL → POL → DEL (15:56)
- **需求**: Trunk Lane 展开看到 CUL Code，CUL 展开看到 total POL，POL 展开看到 DEL 细节
- **修复** (commit 01eb9c0047):
  - **CSS**: `.cul-row` 添加 `cursor:pointer` + `.cul-row .expand-icon` 样式（▶/▼ 旋转动画）
  - **CUL row**: 添加 `onclick="toggleCul(this)"` + `data-cul` + ▶ 图标 → 变成可折叠行
  - **POL TOTAL row**: `data-parent` → `data-parent-cul` → 由 CUL 折叠控制（不再由 Lane 直接控制）
  - **Detail rows**: `data-parent` + `pol-collapsed` → `data-parent-pol` + `hidden` → 由 POL 折叠控制
  - **Single DEL rows**: `data-parent` → `data-parent-cul` → 由 CUL 折叠控制
  - **`toggleCul()`**: 新函数，控制 `[data-parent-cul]` 的 `hidden` + 切换 ▶/▼
  - **`togglePolGroup()`**: 改为控制 `[data-parent-pol]` 的 `hidden`（不再用 `pol-collapsed`）
- **结构**: Lane(row) → CUL(row) → POL(total row) → DEL(detail row)，每层独立折叠，互不干扰

### 2026-06-05 Lane/POL 折叠修复 (15:47)
- **问题**: Lane 不能折叠、POL TOTAL 不能展开看明细
- **根因**:
  1. `.hidden` CSS 只作用于 `.summary-table tr.detail-row.hidden`，CUL 行 `.cul-row.hidden` 和 POL TOTAL 行没有隐藏规则 → toggleLane 对它们无效
  2. POL TOTAL 行缺少 `data-parent` 属性 → toggleLane 根本找不到它
- **修复** (commit 5e8a09f9e3):
  - CSS: `.summary-table tr.detail-row.hidden{display:none}` → `.summary-table tr.hidden{display:none}` 通用化
  - HTML: POL TOTAL 行添加 `hidden` class + `data-parent="lane"` 属性
- **逻辑**: Lane 折叠控制 `hidden`（CUL+POL TOTAL+detail），POL 折叠控制 `pol-collapsed`（仅 detail）。双重 class 独立控制，两状态都为 false 时 detail 才可见

### 2026-06-05 POL 折叠修复 (15:42)
- **问题**: 点击 POL TOTAL 行后整组消失；详情行展开后也看不到
- **根因**: `togglePolGroup()` 用了 `querySelectorAll('[data-pol-group="..."]')` 匹配了所有行，包括 POL TOTAL 行自身（也有 `data-pol-group`），导致 TOTAL 行被加上 `pol-collapsed {display:none}`
- **修复** (commit 8bd3068ab8): 选择器改为 `tr.detail-row[data-pol-group="..."]`，只对详情行操作，不动 TOTAL 行
- **结果**: 点击 POL TOTAL → 详情行展开/折叠；TOTAL 行始终可见；图标 ▶/▼ 正确切换

### 2026-06-05 4 项修复：表头对齐 / ETB 数据 / ETB 筛选 / POL TOTAL (15:08)
- **问题**: Elaine 截图反馈 4 个问题
- **修复** (commit dcf3cba89e):
  1. **表头对齐**: Lane row 和 CUL row 各缺 1 个 ETB `<td>`，从 11 列→12 列
  2. **ETB 数据显示 `-`**: 变量名不匹配 `c.etb`→`c.eta`（buildSummary 中字段名为 `eta`），且改为空白
  3. **ETB 筛选器 UI 断裂**: `<div class="eta-filter">`→`<div class="etb-filter">`（CSS 中只有 `.etd-filter,.etb-filter`）
  4. **POL TOTAL 行**: 新增 POL 分组逻辑，DEL>1 时在顶部插入蓝底 TOTAL 汇总行（ETB 只在 TOTAL 显示），detail 行默认折叠
- **新增 CSS**: `.pol-total-row`（蓝底 #e8f0fe）+ `.pol-collapsed`（默认隐藏）
- **新增 JS**: `togglePolGroup()` 点击 TOTAL 行展开/折叠子行

### 2026-06-05 Data Labels 添加到图表 (14:48)
- **需求**: 三个图表（TEU by POL / TEU by DEL / Volume by DEL）柱状条上显示数据标签
- **改动** (commit 9dc1e8f9bb):
  1. CDN 引入 chartjs-plugin-datalabels@2.2.0
  2. Chart.register(ChartDataLabels) 注册插件
  3. 三图表各加 datalabels 配置：anchor='end', align='end', 颜色匹配柱子（蓝#0f3460/红#e74c3c/绿#27ae60）
  4. TEU: ≥1000 显示为 "12.3k", Volume: ≥1M 显示为 "1.2M", ≥1k显示为 "1.2k"
  5. Y 轴加 grace:'10%' 为标签留空间

### 2026-06-05 Memory 备份到 GitHub (14:00)
- **操作**: 将全部 9 个 memory 文件同步到 `Elaine-Teh/workbuddy-memory`
- **方法**: GitHub REST API PUT，每文件用远端 SHA 增量更新，历史完整保留
- **结果**: 9/9 全部成功（8 update + 1 create）
  - 2026-05-22.md -> SHA e34e3d8ac5
  - 2026-05-23.md -> SHA 4c3b5c0502
  - 2026-05-26.md -> SHA 0622a60b3f
  - 2026-05-28.md -> SHA daa3431d94
  - 2026-06-02.md -> SHA 47d6c2f1d8
  - 2026-06-03.md -> SHA 379a5f5198
  - 2026-06-04.md -> SHA 05605a4877
  - **2026-06-05.md -> SHA 7eedd6d300** (新建)
  - MEMORY.md -> SHA 279cd6a223

### 2026-06-05 数据刷新 #2 (11:43)
- **操作**: 运行 `generate_*.py` → `deploy_data.py` (DATA ONLY)
- **数据量**: 57,808 条记录，44 Lanes，66 POLs，87 DELs，866 CULs
- **ETD 范围**: 2025-06-11 ~ 2026-07-04 | ETB 范围: 2026-01-04 ~ 2026-07-03
- **db_data.json**: 30.7 MB，SHA=db07e8724f
- **version.txt**: 20260605114332
- **index.html**: 未修改 ✅（deploy_data.py DATA ONLY 模式确认安全）

### 2026-06-05 数据刷新 #1 (08:44)
- **操作**: 运行 `generate_daily_booking_dashboard.py` 全量刷新数据
- **数据量**: 57,716 条记录，44 Lanes，66 POLs，87 DELs，862 CULs
- **ETD 范围**: 2025-06-11 ~ 2026-07-03 | ETB 范围: 2026-01-04 ~ 2026-07-03
- **db_data.json**: 30.7 MB，SHA=728bdc9cc9
- **version.txt**: 20260605084632
- **index.html**: ⚠️ deploy_data.py 误触发内联模式覆盖了恢复版！已通过 fix_deploy.py 恢复 fa963e37 基础版本（54.4 KB，DATA_VERSION=20260605084609）
- **deploy_data.py 修复**: 彻底重写为 DATA ONLY 模式，只推 db_data.json + version.txt，**永不再碰 index.html**
- **结论**: 今后 deploy_data.py 安全，不会再覆盖 index.html

### 2026-06-05 Memory 备份到 GitHub (08:43)
- **操作**: 将全部 8 个 memory 文件（2026-05-22 ~ 2026-06-04 + MEMORY.md）同步到 `Elaine-Teh/workbuddy-memory` 仓库
- **方法**: GitHub REST API PUT，每个文件用当前远端 SHA 做增量更新（非 force push，历史完整保留）
- **结果**: 8/8 全部成功（update 模式，无新建文件）
  - 2026-05-22.md -> SHA 69d725a30d
  - 2026-05-23.md -> SHA c3aff274c0
  - 2026-05-26.md -> SHA 124cd12f34
  - 2026-05-28.md -> SHA 1395484697
  - 2026-06-02.md -> SHA 6d49e8f57c
  - 2026-06-03.md -> SHA db9a992807
  - 2026-06-04.md -> SHA 7dffaad156
  - MEMORY.md -> SHA 1e866b057c
